
# 我的小花园 Web 应用代码审查文档

## 1. 审查范围与方法

| 项目 | 说明 |
|---|---|
| 审查对象 | garden.html 全部源码（HTML 结构、CSS 样式、JavaScript 业务逻辑、localStorage 数据层） |
| 审查维度 | 业务逻辑正确性、本地存储可靠性、交互与安全缺陷、可维护性 |
| 审查方法 | 逐功能走读源码，结合边界条件推演与状态机分析 |

## 2. 总体评价

| 维度 | 评价 |
|---|---|
| 优点 | 单文件零依赖交付完整；页面级渲染函数职责清晰；关键路径（待办结转、跨天回补）有防重与去重设计；存储读取有异常兜底；默认数据具备自愈能力 |
| 主要风险 | 部分渲染路径未做 HTML 转义；存储写入无异常保护；删除语义与用户文案存在偏差；原生弹窗与整体风格割裂 |
| 总体结论 | 逻辑骨架健壮，可支撑个人使用场景；进入多用户或多端场景前需优先解决存储写入保护、数据导出与转义覆盖三类问题 |

## 3. 业务逻辑隐患

| 编号 | 模块 | 隐患描述 | 影响 | 严重度 | 优化建议 |
|---|---|---|---|---|---|
| L-01 | 待办完成 | 勾选完成时完成时刻与完成日期固定取实际当天，未关联当前查看日期；回看过去日期补勾任务时，历史完成的"完成日期"显示为实际操作日 | 补记场景下历史完成排序与展示与用户预期不符 | 中 | 勾选时若查看日期为过去，完成日期取查看日期并提示"按查看日期记录" |
| L-02 | 常规任务删除 | 删除按"查看日期起"过滤该模板的全部实例，未排除已完成实例；从今天删除会连带清掉今天及以后已完成记录 | 历史完成统计随之减少，与"历史记录保留"的确认文案存在偏差 | 中 | 过滤条件增加"仅未完成"，已完成实例保留在历史中 |
| L-03 | 特殊任务删除 | 删除任一未完成副本会跨日期删除整条同源未完成链，包括当天的原始任务 | 用户可能只想删明天副本，结果当天原始项一并消失，超出确认弹窗文案的信息量 | 中 | 确认弹窗明确说明"将同时删除之后所有未完成副本"，或提供"仅删本条"选项 |
| L-04 | 特殊任务新增 | 新增弹窗允许选择过去日期；结转机制只扫描前一天，更早日期的未完成任务永远不会被结转 | 过期任务静默失效，用户以为会顺延 | 中 | 选择过去日期时拦截提示，或明确告知"过期任务不再顺延" |
| L-05 | 记账分析 | 日均支出分母为当月已过天数（含未结束的今天）；月初两三天时日均被显著放大 | 数据解读偏差 | 低 | 文案标注"按当月已过天数折算"，或改用自然月天数 |
| L-06 | 记账编辑 | 修改金额输入非法值时静默保留旧值，无任何提示 | 用户以为修改成功 | 低 | 非法输入给出 Toast 提示 |
| L-07 | 提醒机制 | 每轮检查仅展示第一条匹配提醒，依赖关闭后的延时补发下一条；跨天已读重置依赖进入页面时的日期比对 | 行为正确但链式补发较脆弱，时间回拨类操作会导致已读判断异常 | 低 | 改为一次收集全部到期项进行队列化弹窗 |
| L-08 | 跨天回补 | 逐日回补上限 62 天，超过约两个月未打开时更早日期跳过 | 极端场景结转链断档 | 低 | 上限提升或提示用户存在中断区间 |
| L-09 | 体重差值 | 涨跌判断仅以正负区分，等于零落入绿色分支 | 显示为绿色 (0.0)，与"涨红跌绿"直觉不完全一致 | 低 | 增加零值灰色分支 |
| L-10 | 全局日期 | 依赖本机系统时间，用户回拨系统日期可能导致初始化判断错乱、结转重复或跳过 | 数据错乱风险 | 低 | 记录最近一次运行的日期戳做单调性校验 |

## 4. 本地存储风险

| 编号 | 风险描述 | 影响 | 严重度 | 优化建议 |
|---|---|---|---|---|
| S-01 | 写入操作（存储 set）无异常捕获，隐私模式或配额写满时抛错会中断当前业务流程 | 操作无反馈失败，数据丢失 | 高 | 写入包裹异常处理，失败时 Toast 告知并考虑内存降级 |
| S-02 | 多标签页同时打开时同一键最后写入覆盖，无跨页同步监听 | 双开后互相覆盖丢数据 | 中 | 监听存储事件做页间同步或加写入时间戳合并 |
| S-03 | 无导出/导入能力，清除浏览器数据即全量丢失 | 不可恢复的数据损失 | 高 | 提供 JSON 导出/导入作为最低成本备份 |
| S-04 | 存储封装默认值为数组，对象与字符串键依赖各调用方传默认值，防御不统一（初始化处残留针对默认值的类型补丁） | 新增键时容易踩默认值类型坑 | 中 | 封装层支持按键注册类型化默认值 |
| S-05 | 每次读取均整体 JSON 反序列化，渲染过程多次重复读取同一键 | 数据量增大后渲染耗时线性上升 | 低 | 引入内存缓存层，写入时同步更新 |
| S-06 | 历史数据存在数字 id 与新字符串 id 两套体系，依赖字符串化比较兜底 | 规则分散，后续维护易漏 | 低 | 迁移脚本统一 id 体系或收敛比较工具函数 |

## 5. 交互与安全缺陷

| 编号 | 缺陷描述 | 影响 | 严重度 | 优化建议 |
|---|---|---|---|---|
| I-01 | 待办文本与日记内容渲染前已做 HTML 转义，但备忘录标题与类型名、收藏名称与推荐人、记账备注、历史完成标题等路径未转义 | 存储型注入风险，恶意内容可破坏页面结构 | 高 | 抽取统一转义工具函数，覆盖全部用户输入渲染点 |
| I-02 | 编辑与部分删除使用浏览器原生输入框/确认框 | 原生弹窗与治愈系视觉割裂，且个别浏览器禁用原生弹窗后编辑删除不可用 | 中 | 统一替换为自定义输入弹窗组件 |
| I-03 | 自定义日期/时间选择器与三点菜单无键盘可达性与无障碍标注 | 键盘用户与读屏用户不可用 | 中 | 补充焦点管理、回车确认与 aria 标签 |
| I-04 | 三点菜单为行内绝对定位，长任务文本或窄屏下可能溢出卡片边界 | 菜单显示不全或遮挡 | 低 | 靠边自动翻转定位，或改用固定层弹层 |
| I-05 | Toast 为单例覆盖且 2.5 秒消失，连续操作时前一条提示被吞 | 操作结果感知缺失 | 低 | 支持堆叠或多条队列 |
| I-06 | 完成态无独立分区，仅靠划线沉底；回看过去日期时无法区分"当天完成"与"之后补勾" | 回顾场景信息量不足 | 低 | 已完成条目补充完成日期角标 |

## 6. 优化建议优先级

| 优先级 | 建议项 | 对应问题 |
|---|---|---|
| P0 | 存储写入异常保护；HTML 转义覆盖全部渲染路径；数据导出能力 | S-01、I-01、S-03 |
| P1 | 常规任务删除保留已完成实例；删除链确认文案明确化；过期日期拦截；原生弹窗组件化 | L-02、L-03、L-04、I-02 |
| P2 | 键盘可达性、Toast 队列、差值零值分支、日均口径标注、存储类型化默认值 | I-03、I-05、L-09、L-05、S-04 |

---
