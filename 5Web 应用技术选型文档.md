# 我的小花园 Web 应用技术选型文档

## 1. 选型原则与总体架构

| 原则 | 说明 |
|---|---|
| 零依赖 | 不引入任何第三方库、框架或构建工具，保证单文件可长期存活、离线可开 |
| 本地优先 | 全部数据存于浏览器本地存储，无后端、无网络请求，隐私默认安全 |
| 单一交付物 | 一个 HTML 文件即完整产品，便于评审、演示与分发 |
| 可解释优先 | 唯一的数据分析能力采用规则化统计，每个结论可人工复算 |

| 架构层 | 实现方式 |
|---|---|
| 表现层 | 单 HTML 文档+手写 CSS，按页面区块切换显示 |
| 逻辑层 | 原生 JavaScript，按页面拆分渲染函数，操作触发后全量重绘对应区块 |
| 数据层 | localStorage 键值存储+JSON 序列化 |
| 组件层 | 弹窗、Toast、日期/时间选择器均为手写组件 |

## 2. 技术栈清单（仅列真实使用项）

| 层面 | 选型 | 使用位置 | 选择理由 |
|---|---|---|---|
| 文档结构 | HTML5 单文件 | 全站 | 单文件交付，双击即运行 |
| 样式 | 原生 CSS3 | 全站 | CSS 自定义属性统一主题色、Flex 与 Grid 布局、媒体查询做移动端两列适配 |
| 视觉增强 | backdrop-filter、keyframes 动画 | 玻璃卡片、页面淡入、撒花、侧边栏滑出 | 无需动画库即达成治愈系质感 |
| 脚本 | 原生 ES6 JavaScript | 全站逻辑 | 模板字符串拼装视图、箭头函数、解构；不依赖任何框架 |
| 图表 | Canvas 2D API | 体重当月趋势柱状图 | 一张月度柱状图不值得引入图表库，手绘约 40 行更轻 |
| 存储 | Web Storage（localStorage） | 全部业务数据 | 容量满足个人记录场景，同步 API 简单可靠 |
| 日期时间 | 原生 Date 对象 | 全站 | 自行格式化本地日期，避免时区偏移 |
| 定时器 | setInterval / setTimeout | 跨零点检查、Toast 自动消失、撒花清理、提醒链检查 | 浏览器原生能力足够 |

| 明确未使用项 | 说明 |
|---|---|
| 前端框架（React/Vue 等） | 未使用，无虚拟 DOM 与组件化诉求 |
| UI 组件库 | 未使用，弹窗/选择器均为手写以贴合治愈风格 |
| 图表库（ECharts 等） | 未使用，仅一处月度柱状图 |
| 日期库（dayjs 等） | 未使用，日期运算自实现 |
| 网络请求 | 全站零请求，无 fetch/XHR |
| 构建工具与包管理 | 未使用，无打包、无 node_modules |

## 3. 前端实现方案

| 机制 | 说明 |
|---|---|
| 页面切换 | 每个子页面为独立区块，通过激活类切换显示；导航统一入口函数负责关侧边栏、切页面、调渲染、回顶部 |
| 渲染模型 | 每个页面一个渲染函数，数据变更后重绘该页面区块；首页与待办数据联动时双向刷新 |
| 状态读取 | 统一存储读写模块，读取带异常兜底，损坏数据回退默认值 |
| 事件模型 | 行内事件绑定为主，辅以文档级点击监听统一收起弹出菜单与选择器弹层 |
| 输出安全 | 用户输入的任务文本、日记内容在拼入页面 HTML 前统一做转义，防止注入 |

## 4. 数据存储设计（localStorage）

| 存储键（统一前缀 `garden_`） | 内容 |
|---|---|
| todos | 全部待办实例：文本、类型（常规/特殊）、归属日期、完成状态、模板关联、最初添加日期、结转链标识、完成时刻 |
| regularTemplates | 常规任务模板：名称、生效开始日期、删除生效日期 |
| lastTodoDate | 最后一次每日初始化的日期，用于跨天回补 |
| events | 小花备忘录事件：标题、日期、备注 |
| dismissedReminders | 当日已读提醒标记，按天重置 |
| weight | 体重记录：日期、时间（精确到秒）、公斤数值 |
| weightUnit | 体重单位偏好（kg/斤） |
| meals | 用餐记录：餐次、时间、日期 |
| memoTypes | 重复事件类型及其全部打卡记录（精确到秒） |
| diaries | 日记：按日期索引，每天固定 3 条 |
| leisure | 收藏项：类型（书/影）、名称、推荐人、完成状态 |
| expenses | 记账记录：金额、分类、时间、备注、日期 |

| 存储策略 | 说明 |
|---|---|
| 序列化 | 统一 JSON 存取，读取失败回退默认空值 |
| 自愈 | 默认常规模板缺失时自动补回；历史体重记录缺时间字段时自动补齐 |
| 容量与边界 | 单用户个人记录量级远低于 5MB 上限；不做分页与索引 |
| 明确边界 | 换浏览器或清缓存即丢数据，产品上以"本地笔记本"定位承接，不伪装成云产品 |

## 5. 日期与时间处理

| 机制 | 说明 |
|---|---|
| 本地日期基准 | 日期字符串由本地年月日拼接生成，替代 UTC 转换，修复了凌晨 1 点仍显示前一天日期的缺陷 |
| 加减天 | 以当天中午 12 点为锚点做日期加减，规避夏令时与月末边界问题 |
| 日期差 | 通用相差天数函数支撑备忘录徽标、提醒触发、距今天数展示 |
| 自定义选择器 | 浅色日期弹窗（年月翻页+当日花朵标记+选中高亮+确定）与时分下拉弹窗，均为手写组件，全局单例弹层互斥 |
| 跨零点看护 | 每 30 秒比对系统日期，跨天时重置待办与日记的查看日期、同步各选择器并刷新当前页 |

## 6. 记账月底消费分类统计分析（全站唯一智能能力）

| 环节 | 规则 |
|---|---|
| 数据筛选 | 取当月全部记账记录 |
| 汇总指标 | 当月总支出、日均支出（总额除以当月已过天数） |
| 分类统计 | 按 6 个固定分类求和，渲染占比条形图（各分类固定配色）与图例百分比 |
| 重点解读 | 标注占比最高分类及其份额；按总额阈值输出可解释文案（超支提醒/表扬/合理区间） |
| TOP3 | 按单笔金额取前三，优先展示备注、缺省展示分类 |
| 空态 | 当月无记录展示空态提示 |
| 能力定性 | 规则驱动的统计分析，非机器学习；选择可解释规则而非黑盒模型，是面向个人记账场景的刻意取舍 |

## 7. 零依赖策略权衡

| 收益 | 代价 |
|---|---|
| 单文件即产品，评审与演示成本极低 | 无组件复用机制，页面增多后维护成本上升 |
| 离线可用、加载即秒开 | 无路由与代码分割，不适合无限扩页 |
| 不受依赖升级与供应链风险影响 | 测试只能手工进行，缺自动化 |
| 数据不出设备，隐私默认安全 | 无多端同步能力 |

## 8. 安全与隐私

| 项目 | 说明 |
|---|---|
| 数据边界 | 全部数据仅存本机浏览器，无任何上传通道 |
| 注入防护 | 用户输入内容渲染前统一 HTML 转义 |
| 第三方风险 | 零第三方脚本，无追踪、无 CDN 依赖 |
| 敏感信息 | 不收集任何身份信息，无 Cookie |

## 9. 兼容性与性能

| 项目 | 说明 |
|---|---|
| 浏览器要求 | 现代浏览器：需支持 CSS Grid、backdrop-filter、Canvas、ES6 |
| 性能表现 | 无网络请求首屏即现；数据量个人级，全量重绘无感知延迟 |
| 降级策略 | 存储读取全程异常兜底，坏数据不阻塞页面 |

## 10. 演进路线（均为未来选项，当前未实现）

| 方向 | 触发条件 |
|---|---|
| 本地导出/导入 | 用户产生备份诉求时优先做，成本低 |
| PWA 化 | 需要主屏图标与更强离线体验时引入 |
| IndexedDB | 记录量级或字段复杂度上升时替换 localStorage |
| 框架化重构 | 页面超过 10 个或需要多人协作时评估 |
| 账号与云同步 | 产品验证成功后的远期选项，当前明确不做 |
