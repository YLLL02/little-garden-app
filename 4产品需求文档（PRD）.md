# 我的小花园 Web 应用产品需求文档（PRD）

## 1. 文档概述

| 项目 | 说明 |
|---|---|
| 产品名称 | 我的小花园（My Little Garden）Web 应用 |
| 产品形态 | 单文件网页应用，浏览器打开即用，数据保存在本地 |
| 文档依据 | 全部需求严格对应 `garden.html` 已实现的页面、弹窗、跳转与交互，未实现的功能一律不写入 |
| 文档用途 | AI 产品经理面试作品集，展示从用户故事到功能规则的完整产品思维 |
| 目标读者 | 面试官、前端开发、测试 |

## 2. 产品定位与目标

| 项目 | 说明 |
|---|---|
| 一句话定位 | 面向个人用户的"一日生活闭环"轻记录工具：待办、健康、备忘、重复事项、日记、收藏、记账聚合在一个治愈系首页中 |
| 核心差异 | 竞品按功能拆分成多个重应用；本产品按"一天"组织，聚合加治愈激励 |
| 目标一 | 一个首页管完一天：记录任何一项不超过 3 次点击 |
| 目标二 | 打开无负担：无注册、无登录、无网络依赖 |
| 目标三 | 坚持有正反馈：随机鼓励语、完成祝贺、撒花动效、历史完成可回看 |
| 非目标 | 不做社交、不做云同步、不做复杂 BI 报表；全站唯一智能能力为记账的月底消费分类统计分析，不虚构其他 AI 功能 |

## 3. 目标用户与核心场景

| 用户画像 | 核心场景 |
|---|---|
| 学生、初入职场人群 | 早晨打开首页看鼓励语与今日待办；白天勾选常规任务、临时加特殊任务 |
| 记录意愿强但怕麻烦的用户 | 饭后随手记一餐；偶尔称重记录；点击卡片即完成一次重复事项打卡 |
| 有纪念日与提醒需求的用户 | 将待办事项提前记入小花备忘录，依靠站内弹窗在 5 天前与 1 天前各提醒一次 |
| 需要成就感回顾的用户 | 睡前写 3 件开心的事；在历史完成页回看所有已完成事件的开始与完成日期 |
| 记账轻用户 | 当天随手记账，月底点一次"月度分析"看钱花在哪 |

## 4. 信息架构与导航设计

### 4.1 导航规则（硬性约定）

| 规则 | 说明 |
|---|---|
| 无底部 Tab 导航 | 源码不存在底部导航栏，文档与实现保持一致 |
| 一级入口一：首页卡片 | 首页以模块卡片承载 7 个子模块，分两排（4+3），移动端自动变两列 |
| 一级入口二：左滑侧边栏 | 左上角汉堡按钮呼出侧边栏与遮罩，共 8 项：首页、每日待办、健康生活、小花备忘录、重复事件、花花日记、宝藏收藏夹、小金库记账 |
| 返回规则 | 全部子页面左上角均有"← 返回首页"按钮，一键回首页 |
| 特殊入口 | "历史完成"页不在侧边栏中，仅通过首页"历史完成"统计卡进入，属于低频回看型页面 |
| 跳转行为 | 页面切换即关闭侧边栏、切换激活页、调用对应渲染函数并回到页面顶部 |

### 4.2 站点地图

| 页面 | 入口 | 返回按钮 |
|---|---|---|
| 首页 | 默认页、侧边栏 | 不适用 |
| 每日待办 | 首页已完成/未完成统计卡、侧边栏、模块卡片 | 有 |
| 历史完成 | 首页历史完成统计卡（唯一入口） | 有 |
| 健康生活 | 侧边栏、模块卡片 | 有 |
| 小花备忘录 | 侧边栏、模块卡片 | 有 |
| 重复事件 | 侧边栏、模块卡片 | 有 |
| 花花日记 | 首页"今日日记"按钮、侧边栏、模块卡片 | 有 |
| 宝藏收藏夹 | 侧边栏、模块卡片 | 有 |
| 小金库记账 | 侧边栏、模块卡片 | 有 |

## 5. 全局组件与交互规范

| 组件 | 触发场景 | 行为 |
|---|---|---|
| 删除确认弹窗 | 所有删除操作（待办、体重、用餐、备忘、重复事件类型与单条记录、日记单日、收藏、记账） | 统一样式，展示确认文案，提供"取消/删除"两个按钮，删除为二次确认后才执行 |
| 事项提醒弹窗 | 小花备忘录事件到期前 5 天或 1 天 | 花朵图标+标题+说明文字，点击"我知道了"关闭并记录当日已读 |
| 添加任务弹窗 | 待办页两个加号按钮 | 区分"添加常规任务"（仅名称）与"添加特殊任务"（名称+事件日期）两种表单形态 |
| Toast 轻提示 | 新增、保存、删除成功等操作反馈 | 顶部居中滑入，2.5 秒自动消失 |
| 撒花动效 | 日记写满 3 条时 | 全屏彩色纸屑落下后自动清理 |
| 日期选择器 | 所有日期字段 | 浅色弹窗式：年月翻页、星期表头、当日花朵标记、选中高亮、确定按钮 |
| 时间选择器 | 用餐时间、记账时间 | 时/分下拉+确定按钮的浅色弹窗 |

## 6. 功能需求详述

### 6.1 首页

| 模块 | 需求说明 |
|---|---|
| 标题与鼓励语 | 应用标题+随机鼓励语，每次渲染从 24 条文案中随机取一条 |
| 已完成统计卡 | 统计今日待办已完成数，点击进入每日待办页 |
| 未完成统计卡 | 统计今日待办未完成数，点击进入每日待办页 |
| 历史完成统计卡 | 统计全部已完成待办总数，点击进入历史完成页 |
| 今日日记按钮 | 快捷进入花花日记页 |
| 模块卡片 | 第一排：每日待办、健康生活、小花备忘录、重复事件；第二排：花花日记、宝藏收藏夹、小金库记账 |

### 6.2 每日待办

| 功能 | 需求说明 |
|---|---|
| 查看日期导航 | 日期选择器两侧各一个圆形尖括号按钮，点击可跳至上一天/下一天；日期也可通过选择器任意跳转 |
| 常规任务区 | 浅绿底分区，基于模板每日自动生成；默认模板为体能训练、肌肉拉伸、眼睛训练、表达能力 |
| 常规任务新增 | 弹窗仅填名称；新增即新增模板，当天生效，此后每日生成 |
| 常规任务编辑 | 修改文本时同步更新模板与查看日期之后的未完成实例，历史日期文本保留 |
| 常规任务删除 | 二次确认后，从查看日期（若查看过去日期则从今天）起不再生成，并即时清除当天及之后未完成实例；历史日期记录保留 |
| 特殊任务区 | 浅黄底分区；未来日期打开时默认为空态，不预生成任何结转副本 |
| 特殊任务新增 | 弹窗填名称+事件日期；新增后视图自动跳到该日期 |
| 特殊任务结转 | 仅在进入新的一天时，将前一天未完成、非备忘转入的特殊任务复制到当天；结转项保留最初添加日期并以"xx添加"小标签展示 |
| 特殊任务终止 | 结转项当天完成后，次日不再出现；删除任一结转项即删除其整条未完成结转链 |
| 备忘转入 | 小花备忘录事件到期前一天，自动生成一条"�� 事件名"的特殊任务，不参与结转链与添加日期标签 |
| 完成态展示 | 不设独立"已完成"分区；完成项划线置灰并沉底 |
| 排序规则 | 常规区：未完成在前、完成沉底；特殊区：当天新增在前、结转项按最初添加日期倒序居中、完成沉底 |
| 行内操作 | 每行右侧仅显示不显眼的横排三个小点，点击弹出"编辑/删除"菜单，点击空白处收起 |
| 完成反馈 | 勾选后随机播报 6 条祝贺语之一，首页统计同步刷新 |

### 6.3 历史完成

| 功能 | 需求说明 |
|---|---|
| 数据范围 | 全部已完成待办，含常规与特殊 |
| 排序 | 按完成时间倒序，最新完成的在最上面 |
| 字段 | 事件名、类型（常规/特殊）、开始日期（最初添加日）、完成日期 |
| 交互 | 纯回顾列表，无编辑删除操作 |

### 6.4 健康生活

| 功能 | 需求说明 |
|---|---|
| 体重记录 | 日期选择+数值输入（默认提示 120）+记录按钮，记录时间精确到秒 |
| 单位切换 | kg 与斤点击切换并持久化，列表数值与涨跌差值随单位换算 |
| 体重列表 | 按时间倒序，每条显示与上一条的涨跌：上涨红色、下降绿色、带正负差值；支持编辑与删除 |
| 体重趋势 | 点击"查看当月趋势"展示当月柱状图，每天取当天最新一条；当月无数据显示"本月暂无体重数据" |
| 用餐记录 | 餐次四选一（早餐/午餐/晚餐/加餐）+时间选择+一键记录，日期固定为当天 |
| 用餐列表 | 仅展示当天记录并按时间排序，支持删除 |

### 6.5 小花备忘录

| 功能 | 需求说明 |
|---|---|
| 新增事件 | 标题必填，日期与备注可选；列表按日期从近到远排序 |
| 日期徽标 | 今天红色高亮；未来 5 天内显示"X天后"；已过期置灰 |
| 站内提醒 | 进入应用时检查：到期前 5 天弹"五天提醒"，前 1 天弹"明天提醒"；同一事件同一天只提醒一次，关闭即记录已读，次日重置 |
| 提醒联动 | 关闭"明天提醒"后立即初始化当日待办，将事件转入特殊任务 |
| 维护 | 支持编辑标题与备注、删除事件 |

### 6.6 重复事件

| 功能 | 需求说明 |
|---|---|
| 类型管理 | 自定义类型（如大姨妈、洗头发），名称防重复；支持改名与整类型删除（含全部记录，需确认） |
| 记录方式 | 点击类型卡即记录当前精确到秒的时间，极简打卡 |
| 卡片展示 | 类型名、最近一次记录时间、距今天数 |
| 历史记录 | 按类型分组、组内倒序展示每条时间与距今天数，单条可删除 |

### 6.7 花花日记

| 功能 | 需求说明 |
|---|---|
| 日期选择 | 顶部提供日期选择器，可查看之前日期的日记并补写 |
| 编辑区 | 3 行星星输入框，对应 3 件开心的事，回填所选日期已有内容 |
| 保存规则 | 至少填写 1 条方可保存；写满 3 条触发全屏撒花与祝贺文案，否则提示已保存条数 |
| 历史日记 | 按日期倒序展示每天内容，当天带花朵标记，支持按天删除 |

### 6.8 宝藏收藏夹

| 功能 | 需求说明 |
|---|---|
| 分类切换 | 书单/影单双 Tab，标题旁显示当前 Tab 待读/待看数量 |
| 新增 | 名称必填，推荐人可选，归入当前所在 Tab |
| 列表规则 | 未完成在上，已完成置灰置底并带完成标记 |
| 维护 | 未完成可一键"完成"，已完成可"撤销"；支持编辑名称与推荐人、删除 |

### 6.9 小金库记账（全站唯一数据分析模块）

| 功能 | 需求说明 |
|---|---|
| 记账字段 | 金额必填（校验大于 0）、6 个固定分类（餐饮/购物/交通/娱乐/学习/其他）、时间选择、备注可选；日期固定为当天，不支持补记往日 |
| 列表 | 按日期时间倒序，显示金额、分类、时间、备注；支持编辑金额与备注、删除 |
| 月度分析 | 点击"月度分析"在原位展开分析卡：当月总支出、日均支出、分类占比条形图与图例百分比、占比最高分类解读、TOP3 支出（金额前三） |
| 解读规则 | 基于固定阈值的可解释文案：总额超 3000 提示节制，低于 1000 给予表扬，其余提示合理；无数据展示空态 |
| 能力边界 | 本模块为全站唯一数据智能能力，采用规则化统计而非机器学习，保证每个数字可追溯、可解释 |

## 7. 核心状态与数据规则

| 规则 | 说明 |
|---|---|
| 每日初始化 | 进入应用或跨天时，按天补齐：常规任务生成、备忘事件转入、特殊任务结转，并更新"最后初始化日期"标记；中断多日后逐天回补，保证链条不丢 |
| 未来日期隔离 | 特殊任务只对"今天"初始化；查看未来日期时清理系统自动生成的未来副本，保证未来特殊区为空；用户手动添加到未来日期的任务除外 |
| 结转规则 | 仅扫描前一天未完成项；同源任务（同一结转链）在目标日已有副本则不重复生成；完成后链条自然终止 |
| 完成记录 | 勾选完成时记录完成时刻（精确到秒）与完成日期，撤销完成时清除，供历史完成页排序与展示 |
| 过零点刷新 | 页面常驻定时器每 30 秒检查一次系统日期；跨 0 点后自动将待办查看日期、日记日期等重置为新一天并刷新当前页面 |
| 本地日期基准 | 日期计算采用浏览器本地时间拼接，而非 UTC 转换，避免凌晨时段显示前一天日期的错误 |
| 删除语义 | 常规任务删除面向未来生效、保留历史；特殊任务删除清除整条未完成结转链；其余模块删除均为单条物理删除，全部需二次确认 |

## 8. 非功能需求

| 类别 | 需求说明 |
|---|---|
| 性能 | 单文件、零外部请求，打开即用；列表按需渲染 |
| 可用性 | 所有空状态提供表情图标+引导文案；按钮大尺寸大圆角，适合移动端点按 |
| 视觉 | 草绿色+米色+木色治愈风；半透明玻璃卡片；花朵表情点缀；浅色日期/时间选择器 |
| 兼容 | 面向现代浏览器；移动端栅格自适应 |
| 隐私 | 全部数据仅存本地浏览器，不经网络传输 |

## 9. 验收要点

| 编号 | 场景 | 预期结果 |
|---|---|---|
| A1 | 点击日期导航进入未来日期 | 特殊任务区为空态，无自动结转副本 |
| A2 | 今天新增特殊任务且未完成 | 明天首次打开时出现在明天列表，带最初添加日期标签，排在明天新增项之后 |
| A3 | 明天完成该结转任务 | 后天不再出现 |
| A4 | 删除一条结转任务 | 整条未完成结转链同时消失 |
| A5 | 删除常规任务 | 当天即时消失，此后不再生成，过去日期记录保留 |
| A6 | 勾选完成任务 | 划线沉底，首页已完成+1，历史完成页新增一条且排最上 |
| A7 | 事件设为 5 天后或明天 | 进入应用弹出对应提醒，当日关闭后不再重复 |
| A8 | 系统时间跨 0 点 | 30 秒内日期自动刷新为新一天 |
| A9 | 记账两笔后点月度分析 | 总额等于两笔之和，分类占比与 TOP3 正确 |

## 10. 迭代建议（当前版本均未实现）

| 方向 | 说明 |
|---|---|
| 补记能力 | 记账支持选择过去日期、重复事件支持补录，当前均固定为当下 |
| 数据导出 | 提供本地数据导出备份，降低换设备丢失风险 |
| 预算能力 | 在现有月度分析上叠加分类预算线与月环比，延续可解释规则路线 |
| 云同步 | 引入账号体系，作为远期选项而非当前承诺 |

---
